[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node_modules/.bin/http-server build -p $PORT -a 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
